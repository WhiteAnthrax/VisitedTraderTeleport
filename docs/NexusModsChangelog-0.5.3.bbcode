Added [b]perMeter[/b] travel cost configuration for finer distance-based tuning while keeping older [b]perKilometer[/b] configs compatible.

Destination choices now show the configured travel cost before selecting a trip.

Added better diagnostics for the optional travel transition sound. If a configured sound key is not loaded by the game, the mod now writes a warning to the log.

No save reset is required.
